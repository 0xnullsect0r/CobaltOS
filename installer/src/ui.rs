//! GUI installer built with iced.

use anyhow::Result;

pub fn run() -> Result<()> {
    // TODO: implement iced GUI application
    // The GUI will drive the InstallStep state machine defined in installer.rs
    println!("CobaltOS Installer — GUI mode (iced) [not yet implemented]");
    println!("Run with DISPLAY or WAYLAND_DISPLAY unset for TUI mode.");
    Ok(())
}
